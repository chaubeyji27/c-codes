#include<stdio.h>
int main() {
   int l,b, area;
   printf("enter the length and breadth of the rectangle");
   scanf("%d%d", &l, &b);
   area = l * b;
   printf("the area of the rectangle is %d", area);
    return 0;
}